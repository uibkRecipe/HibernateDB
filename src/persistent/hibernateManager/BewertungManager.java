package persistent.hibernateManager;

import org.hibernate.Session;

public class BewertungManager extends PersistentManager{

	public BewertungManager(Session session) {
		super(session);
	}
	/* .... */
}
